use flake

if [ ! -d "venv" ]; then
  python -m venv venv
  source venv/bin/activate
  pip install -r requirements.txt l> /dev/null
else
  source venv/bin/activate
fi

layout_deactivate() {
	deactivate
}
