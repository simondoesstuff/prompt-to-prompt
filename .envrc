use flake

venv=venv311
requirements=req_flux.txt

if [ ! -d $venv ]; then
  python -m venv $venv
  source $venv/bin/activate
  pip install -r $requirements l> /dev/null
else
  source $venv/bin/activate
fi

layout_deactivate() {
	deactivate
}
